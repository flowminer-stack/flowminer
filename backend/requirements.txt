fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
psycopg2-binary
alembic
pydantic>=2.4
pydantic-settings
# python-jose <3.3.0 is vulnerable to CVE-2024-33663 (algorithm
# confusion — `alg:none` JWT bypass).
python-jose[cryptography]>=3.3.0
passlib[bcrypt]
bcrypt==4.1.3
python-multipart
celery[redis]
redis
# Cron-expression parsing for connector auto-sync scheduling (scan_connector_schedules
# in workers/tasks.py). The connector UI emits 5-field cron strings; without this the
# scheduled-sync half of the close-the-loop never fires.
croniter>=2.0.0
# PINNED. pm4py MUST be a modern (>=2.7) release: only these read the
# OCEL 2.0 JSON/XML/SQLite formats the OCPM feature relies on (via
# ``read_ocel2*``). Older 2.2.x silently returns ``None`` for OCEL 2.0
# files, which surfaced as "OCEL not found" in the demo after a rebuild
# resolved pm4py down to 2.2.32 (dragged there by ocpa's hard pin — see
# below). 2.7.x also accepts ``discover_dfg(case_id_key=...)`` which the
# 2.2.x line rejected. Keep this pinned so image rebuilds can't drift.
pm4py==2.7.22.4
pandas<3.0
numpy
# Parquet engine. Many connectors (SAP, database, snowflake, bigquery, and the
# REST/ERP connectors) serialise their fetched data with ``df.to_parquet(...)``,
# which pandas implements ONLY via pyarrow/fastparquet — neither is pulled in
# transitively, so without this the parquet-writing connectors ImportError at
# runtime ("Unable to find a usable engine"). Pinned explicitly; a parquet
# round-trip test in tests/connectors guards it.
pyarrow>=15
openpyxl
aiofiles
httpx
python-dotenv
scipy
scikit-learn
# SHAP feature attributions for the predictive monitoring explainability
# endpoint (/predict/explain). Imported LAZILY inside predictive_service so
# the module still imports when shap is absent (the endpoint then returns a
# clean {available: false, ...}); ship it so explanations actually run.
shap>=0.46.0
pytest
pytest-asyncio
aiosqlite
# Test-only: mock httpx requests so connector tests can replay realistic API
# responses (Jira/SAP OData/REST facades) without a live source. Pure-Python.
respx
# Test-only: synthetic relational data generator for the log-join tests
# (tests/fixtures/generate_p2p_data.py emits EKKO/EKPO/EKBE-shaped header/line/
# history tables). Deterministic; used for cosmetic vendor names only.
faker
# Test-only: record-once / replay-forever HTTP cassettes (vcrpy) for the live
# connector tests against free dev orgs (GitHub/Salesforce/ServiceNow/Jira).
# Default record mode is "none" so normal runs never hit the network.
pytest-recording
# Generates TPC-H relational data locally at any scale factor (CALL dbgen) to
# stress the multi-table log-join (orders<->lineitem on orderkey). Used by the
# integration-tier tests/connectors/test_log_join_tpch.py (importorskip'd) AND
# by demo_seeder to build the bundled TPC-H demo project at boot.
duckdb
structlog
sentry-sdk[fastapi]
slowapi
email-validator
reportlab
lingam
cryptography
# LLM providers — used by app/services/llm.py. Keeping these optional
# at runtime means the null-provider fallback still works without them,
# but ship them in the base image so configured providers actually run.
openai
anthropic
# Anthropic's Model Context Protocol SDK — backs the optional
# ``flowminer-mcp`` server in app/mcp/ which exposes the mining
# endpoints as MCP tools for Claude Desktop, Cursor, Zed, etc.
mcp
# Backs the operator CLI at app/cli.py (``python -m app.cli``) —
# tiny click-based framework for the user-management subcommands.
typer>=0.12,<0.16
# Discrete-event simulation engine (Task 2 / DES endpoint).
# MIT-licensed, pure-Python, zero heavy deps — safe to add unconditionally.
simpy
# Object-Centric Process Analytics — backs the OPerA object-centric
# performance metrics endpoint in app/api/ocel.py (synchronization /
# pooling / lagging / flow time via token-replay). Imported LAZILY
# inside the endpoint (mirroring the optional LLM providers above) so
# `import app.main` still works if ocpa isn't installed; the endpoint
# returns a clear 501 in that case instead of crashing the app.
#
# NOT INSTALLED BY DEFAULT. ocpa==1.3.4 (its latest release) hard-pins
# ``pm4py==2.2.32``, which cannot read OCEL 2.0 files — installing it
# alongside this requirements set forces pm4py back down and breaks the
# core OCPM feature (the "OCEL not found" demo regression). Core OCEL 2.0
# support outweighs one optional advanced-metrics endpoint, so ocpa is
# left out of the default image; the OPerA endpoint degrades to a clean
# 501. To enable it, install ocpa into an ISOLATED environment with its
# own pinned pm4py 2.2.32 — it is incompatible with pm4py 2.7 in-process.
# ocpa  # see note above — do not re-add without resolving the pm4py pin conflict

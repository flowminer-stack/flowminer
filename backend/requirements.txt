fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
psycopg2-binary
alembic
pydantic>=2.4
pydantic-settings
# python-jose <3.3.0 is vulnerable to CVE-2024-33663 (algorithm
# confusion — `alg:none` JWT bypass).
python-jose[cryptography]>=3.3.0
passlib[bcrypt]
bcrypt==4.1.3
python-multipart
celery[redis]
redis
pm4py
pandas<3.0
numpy
openpyxl
aiofiles
httpx
python-dotenv
scipy
scikit-learn
pytest
pytest-asyncio
aiosqlite
structlog
sentry-sdk[fastapi]
slowapi
email-validator
reportlab
lingam
cryptography
# LLM providers — used by app/services/llm.py. Keeping these optional
# at runtime means the null-provider fallback still works without them,
# but ship them in the base image so configured providers actually run.
openai
anthropic
# Anthropic's Model Context Protocol SDK — backs the optional
# ``flowminer-mcp`` server in app/mcp/ which exposes the mining
# endpoints as MCP tools for Claude Desktop, Cursor, Zed, etc.
mcp
# Backs the operator CLI at app/cli.py (``python -m app.cli``) —
# tiny click-based framework for the user-management subcommands.
typer>=0.12,<0.16
# Discrete-event simulation engine (Task 2 / DES endpoint).
# MIT-licensed, pure-Python, zero heavy deps — safe to add unconditionally.
simpy
